interface Props {
  images: any,
  colors: any,
  name: string,
  priceRange: any,
};

export default function ProductCard(props: Props) {
  return (
  <div>
    <img src="" alt="" />
    
  </div>);
}